# shellcheck shell=bash disable=SC2034
_sb2dir() {
    local cur prev words cword
    local word option
    _init_completion -n = || return

    for word in "${words[@]}"; do
        [[ $word != --help && $word != --version ]] || return
    done

    case $prev in
    -b | --bext)
        mapfile -t COMPREPLY < <(compgen -W 'sb sqfs sfs' -- "$cur")
        return
        ;;
    esac

    if [[ $cur == *=* ]]; then
        option=${cur%%=*}
        cur=${cur#*=}
        case $option in
        --bext)
            mapfile -t COMPREPLY < <(compgen -W 'sb sqfs sfs' -- "$cur")
            return
            ;;
        esac
    fi

    if [[ $cur == --* ]]; then
        mapfile -t COMPREPLY < <(compgen -W \
            '--bext --keep-ownership --allow-special --json --no-color --help --version' -- "$cur")
    elif [[ $cur == -* ]]; then
        mapfile -t COMPREPLY < <(compgen -W \
            '-b --bext --keep-ownership --allow-special --json --no-color --help --version' -- "$cur")
    else
        local IFS=$'\n'
        mapfile -t COMPREPLY < <(compgen -f -X '!*.sb' -- "$cur")
        mapfile -O "${#COMPREPLY[@]}" -t COMPREPLY < <(compgen -d -- "$cur")
    fi
}

complete -F _sb2dir sb2dir
